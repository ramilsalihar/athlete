import 'package:flutter/material.dart';

class DailyChallengeSuccess extends StatelessWidget {
  const DailyChallengeSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
